<form method="post" action="{{ path('app_inventar_delete', {'id': inventar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inventar.id) }}">
    <button class="btn">Delete</button>
</form>
